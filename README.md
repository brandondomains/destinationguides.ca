# DestinationGuides.ca (Jekyll) — V2 design + content seeded

Permalinks:
- /countries/{slug}/
- /regions/{slug}/
- /cities/{slug}/
- /attractions/{slug}/
- /practical/{slug}/

## Run locally
```bash
bundle install
bundle exec jekyll serve
```
